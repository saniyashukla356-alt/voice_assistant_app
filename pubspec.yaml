name: vaani_ai
description: "A Voice Assistant App called Vaani AI"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.2
  speech_to_text: ^7.4.0 # 👈 Update this to the latest version
  http: ^1.2.1
  flutter_tts: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
